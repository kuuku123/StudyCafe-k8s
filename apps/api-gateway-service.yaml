apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: studycafe
  labels:
    app: api-gateway
spec:
  type: NodePort
  ports:
    - port: 8083
      targetPort: 8083
      nodePort: 30083
      protocol: TCP
  selector:
    app: api-gateway
