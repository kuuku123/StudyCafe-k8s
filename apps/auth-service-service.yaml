apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: studycafe
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
  selector:
    app: auth-service
