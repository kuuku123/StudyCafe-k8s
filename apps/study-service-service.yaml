apiVersion: v1
kind: Service
metadata:
  name: study-service
  namespace: studycafe
  labels:
    app: study-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  selector:
    app: study-service
