apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: studycafe
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
  selector:
    app: notification-service
