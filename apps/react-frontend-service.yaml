apiVersion: v1
kind: Service
metadata:
  name: react-frontend
  namespace: studycafe
  labels:
    app: react-frontend
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
  selector:
    app: react-frontend
