apiVersion: v1
kind: Service
metadata:
  name: controller-1
  namespace: studycafe
  labels:
    app: kafka-controller
spec:
  type: ClusterIP
  ports:
    - port: 9093
      targetPort: 9093
      protocol: TCP
  selector:
    app: kafka-controller
